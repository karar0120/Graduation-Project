
import 'package:advanced/app/app.dart';
import 'package:flutter/material.dart';

class data extends StatelessWidget {
  const data({Key? key}) : super(key: key);

  void getdata(){
    print (MyApp.insetance.Ahmed);
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
